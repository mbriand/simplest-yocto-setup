header:
  version: 11

machine: krazymp

repos:
  meta-openembedded:
    url: "https://git.openembedded.org/meta-openembedded.git"
    branch: wrynose
    layers:
      meta-oe:
      meta-python:

  meta-kiss-amd:
    layers:
      meta-kiss-amd:

local_conf_header:
  meta-kiss-amd: |
    LICENSE_FLAGS_ACCEPTED += "commercial_sdtgen-native"
